console.log("Hello, TypeScript!");

let userName: string;
let userAge: number;
let userStatus: boolean;
function add(a: number, b: number, ) { }
function add_alt(a: number, b = 5, ) { }

// ...

userName = 'Max';
userAge = 30;
userStatus = true;
add(10, 20);